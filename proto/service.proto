syntax = "proto3";

package internal.rpc.proto;

option go_package = "internal/rpc/proto";

message MetricMessage {
  string name = 1;
  oneof value {
    uint64 counter = 3;
    double gauge = 4;
  }
}

message UpdateMetricResponse {
  MetricMessage metric = 1;
}

service GoDevops {
  rpc UpdateMetric(MetricMessage) returns (UpdateMetricResponse);
}